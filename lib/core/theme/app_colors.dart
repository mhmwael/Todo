import 'package:flutter/material.dart';

// Centralized color palette used throughout the app for consistency
class AppColors {
  // Primary brand colors
  static const Color primary = Color(0xFFFAB589); // Main brand orange
  static const Color primaryDark = Color(0xFFEB7A34); // Darker orange variant
  static const Color accent = Color(0xFF42A5F5); // Secondary blue accent

  // Priority level indicators
  static const Color highPriority = Color(0xFFE53935); // Red for urgent tasks
  static const Color mediumPriority = Color(0xFFFFA500); // Orange for medium
  static const Color lowPriority = Color(0xFF4CAF50); // Green for low priority

  // Text colors
  static const Color
  background = Color(
    0xFFFAFAFA,
  );
  static const Color
  surface = Colors.white;
  static const Color
  textPrimary = Color(
    0xFF212121,
  );
  static const Color
  textSecondary = Color(
    0xFF757575,
  );
  static const Color
  divider = Color(
    0xFFEBEBEB,
  );
}
