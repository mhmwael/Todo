import 'package:flutter/material.dart';

class AppColors {
  static const Color
  primary = Color(
    0xFFFAB589,
  ); // Splash screen color
  static const Color
  primaryDark = Color(
    0xFFEB7A34,
  ); // Darker splash variant
  static const Color
  accent = Color(
    0xFF42A5F5,
  );

  static const Color
  highPriority = Color(
    0xFFE53935,
  );
  static const Color
  mediumPriority = Color(
    0xFFFFA500,
  );
  static const Color
  lowPriority = Color(
    0xFF4CAF50,
  );

  static const Color
  background = Color(
    0xFFFAFAFA,
  );
  static const Color
  surface = Colors.white;
  static const Color
  textPrimary = Color(
    0xFF212121,
  );
  static const Color
  textSecondary = Color(
    0xFF757575,
  );
  static const Color
  divider = Color(
    0xFFEBEBEB,
  );
}
